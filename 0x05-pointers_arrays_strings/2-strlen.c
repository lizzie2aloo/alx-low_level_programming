/**
 * _strlen - returns tge length of s string
 * @s: string parameter input
 * Return: Length of a spring
 */
	
int _strlen(char *s)
{
	char str[];
	int = s

	for (s = 0; str[s] != '/0'; ++s);

	return (s)
}
