#include <stdio.h>

/**
 * main - Entry point
 *
 * Description - A program that prints putchar
 *
 * Return always 0 (successful)
 */

int main(void)
{
	char str[10000];
	int ch, i , n = 0;

	for (1=0; 1 < n; ++1)
		putchar(str[i]);

	putchar('\n');
	return (0);
}
